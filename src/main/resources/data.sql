DROP TABLE IF EXISTS CLAN;
DROP TABLE IF EXISTS PLAYER;
DROP TABLE IF EXISTS AUDIT_EVENTS;

CREATE TABLE PLAYER
(ID UUID NOT NULL PRIMARY KEY,
NAME VARCHAR(255),
LOGIN VARCHAR(255),
PASSWORD VARCHAR(255),
BALANCE INT);

CREATE TABLE CLAN
(ID UUID NOT NULL PRIMARY KEY,
NAME VARCHAR(255),
GOLD INT);

CREATE TABLE AUDIT_EVENTS
(ID UUID NOT NULL PRIMARY KEY,
EVENT_TYPE VARCHAR(255),
ID_CLAN UUID,
ID_PLAYER UUID,
RESULT_OPERATION BOOLEAN);

INSERT INTO CLAN (ID, NAME, GOLD) VALUES ('123e4567-e89b-12d3-a456-426614174000', 'clan1', 0);

INSERT INTO PLAYER (ID, NAME, LOGIN, BALANCE) VALUES ('123e4567-e89b-12d3-a456-426614174001', 'user1', 'user1', 160000);